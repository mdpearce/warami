CREATE TABLE postSearchParams (
	id	INTEGER NOT NULL,
	listingType	TEXT,
	sortType	TEXT,
	page	INTEGER,
	postLimit	INTEGER,
	communityId	INTEGER,
	communityName	TEXT,
	isSavedOnly	INTEGER,
	PRIMARY KEY(id)
);

insert:
INSERT INTO postSearchParams (
    id,
    listingType,
    sortType,
    page,
    postLimit,
    communityId,
    communityName,
    isSavedOnly
) VALUES ?;
